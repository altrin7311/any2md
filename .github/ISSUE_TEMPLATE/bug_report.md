---
name: Bug report
about: Something didn't convert the way you expected
title: ""
labels: bug
---

**What you converted**
The URL or file type (don't paste private content).

**What happened**
The terminal output, including any `⚠` warning lines. Paste the text, not a screenshot.

**What you expected**

**Environment**
- Any2MD version (`any2md --version`):
- Install method (`pipx` / `pip -e` / Docker):
- OS:
- Summarizer (`extractive` / `ollama` / `none`):

**Note:** Any2MD is fully offline and uses no API keys. If a web page came out empty, it may be
paywalled or JavaScript-only — mention if so.
